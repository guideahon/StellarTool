Portable GUI + CLI; bundles repak, retoc, UAssetGUI and cue4parse. Requires .NET 10 Desktop Runtime x64 for UAssetGUI (SDK also works, but isn't needed). Extract and run; no game files touched. Source: github.com/guideahon/StellarTool
